pragma solidity 0.8.0;

contract Test {
    modifier owned_by(address a) {}
    modifier OwnedBy(address a) {}
    modifier _OwnedBy(address a) {}

    modifier ownedBy(address a) {}
    modifier _ownedBy(address a) {}
}